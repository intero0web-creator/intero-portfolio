// Тихая пасека — единый config сайта.
// Меняете значения здесь — они автоматически подтянутся на всех страницах.

window.CFG = {
  // === БРЕНД И КОНТАКТЫ ===
  brand:        'Тихая пасека',
  // Канонический URL сайта — для OG-тегов, ссылок в письмах и подобного.
  // ВНИМАНИЕ: сами OG-теги в HTML-файлах статичны, поменяйте <meta property="og:url">
  // и <meta property="og:image"> руками, когда узнаете реальный домен.
  siteUrl:      'https://tihaya-paseka.ru',

  phone:        '+7 (999) 123-45-67',
  phoneRaw:     '+79991234567',            // без пробелов — для tel:
  phoneHint:    'ежедневно с 9:00 до 21:00 по московскому времени',
  email:        'hello@tihaya-paseka.ru',
  telegram:     '@tihaya_paseka',
  telegramUrl:  'https://t.me/tihaya_paseka',
  address:      'Костромская область, Судиславский район, дер. Медведки',

  // Соцсеть VK — оставьте пустой строкой, если аккаунта нет: пункт скроется автоматически.
  // Пример заполнения: vk: 'vk.com/tihayapaseka', vkUrl: 'https://vk.com/tihayapaseka'
  vk:           '',
  vkUrl:        '',

  // Реквизиты (для страницы контактов)
  legalName:    'ИП Иванов В. С.',
  inn:          '4401234567',
  ogrn:         '320440000012345',

  // === ОТПРАВКА ФОРМ ===
  // Скрипт выбирает первый настроенный вариант из трёх:
  //   1) LEAD_URL — шлём JSON туда (Formspree / Getform / свой backend)
  //   2) TG_TOKEN + TG_CHAT — шлём напрямую в Telegram-бота
  //   3) DEMO=true — форма имитирует отправку, ничего не шлёт (для тестов)

  // Вариант A: сторонний сервис форм.
  // Пример для Formspree: 'https://formspree.io/f/xxxxxxxx'
  LEAD_URL:     '',

  // Вариант B: свой Telegram-бот.
  // TG_TOKEN — токен от @BotFather.
  // TG_CHAT  — chat_id получателя (узнать: напишите боту @userinfobot).
  // ВНИМАНИЕ: токен окажется в клиентском коде, кто угодно сможет его прочитать.
  // Для боевого проекта проксируйте через минимальный serverless-эндпоинт.
  TG_TOKEN:     '',
  TG_CHAT:      '',

  // Демо-режим. Пока не настроен LEAD_URL или Telegram — оставьте true.
  // Пользователь увидит успешное сообщение с пометкой «(демо)» в консоли будет payload.
  DEMO:         true
};
